<div className="feature-cards" style={{backgroundColor:bgColor}}>
  <div className="card tall-card">
    <div className="icon">💲</div>
    <h3>Zero Infrastructure Cost</h3>
    <p>Track locations without investing in expensive GPS hardware.</p>
  </div>

  <div className="card">
    <div className="icon">📍</div>
    <h3>Precise Location</h3>
    <p>Highly accurate positioning powered by GPS technology.</p>
  </div>

  <div className="card">
    <div className="icon">⚡</div>
    <h3>Real-time Tracking</h3>
    <p>Monitor E-Vans live with instant location updates on your device.</p>
  </div>

  <div className="card">
    <div className="icon">🚌</div>
    <h3>Multi-Van System</h3>
    <p>View the real-time locations of multiple E-Vans on one system.</p>
  </div>
</div>



and 


.feature-cards {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    margin-top: 20px;
    gap: 25px;
    padding: 0 15px;
  }

.feature-cards {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  margin-top: 20px;
  gap: 25px;
  padding: 0 15px;
}

.feature-cards .card {
  flex: 1 1 250px;     /* flexible but min width = 250px */
  max-width: 450px;    /* card won't grow beyond this */
  background: #1e293b;
  border-radius: 12px;
  padding: 25px;
  transition: transform 0.3s ease, box-shadow 0.3s ease;
  cursor: pointer;
  color: white;
}

.feature-cards .card:hover {
  transform: translateY(-6px) scale(1.02);
  box-shadow: 0 8px 20px rgba(0,0,0,0.35);
}

.feature-cards .card h3 {
  font-size: 1.2rem;
  margin: 12px 0;
  font-weight: bold;
}

.feature-cards .card p {
  font-size: 0.95rem;
  color: #cbd5e1;
  margin: 0;
}

.feature-cards .icon {
  font-size: 1.8rem;
  margin-bottom: 8px;
}

/* Special taller card */
.tall-card {
  height: 900px;
}

/* Responsive rules */
@media (max-width: 768px) {
  .feature-cards {
    flex-direction: column;
    align-items: center;
  }
  .feature-cards .card {
    width: 80vw;
  }
  
}